{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "node scripts/prepare-static-images.mjs && nuxt generate && node scripts/generate-static-images.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck"
  },
  "devDependencies": {
    "@nuxt/image": "^2.1.0",
    "@nuxtjs/robots": "^6.1.3",
    "@nuxtjs/sitemap": "^8.3.0",
    "nuxt": "^3.21.10",
    "nuxt-schema-org": "^6.2.8",
    "sharp": "^0.35.3",
    "typescript": "^5.6.0",
    "vue-tsc": "^3.3.8"
  }
}
